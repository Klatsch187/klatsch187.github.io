---
/**
 * About Me — landing page. Editorial masthead hero + the shared About body
 * (data-viz + timeline). The body lives in AboutSections to keep this tidy.
 */
import BaseLayout from '../layouts/BaseLayout.astro';
import Hero from '../components/Hero.astro';
import AboutSections from '../components/AboutSections.astro';
---

<BaseLayout title="About" activeNav="about">
  <Hero />
  <AboutSections />
</BaseLayout>
